# 品質ゲートレポート（Phase 5.5）

**実施日**: 2026-05-09
**ポリシー**: strict
**対象**: petit-event-maker（本番稼働中）

---

## 🟥 1. Mock検出（strict policy）

### Critical: なし ✅
- ハードコード認証情報なし
- 本番APIキーの露出なし
- TODO/FIXME 0件

### High: 1件 🟡
| 検出 | 件数 | 場所 | 影響 |
|---|---|---|---|
| `localhost:3007` フォールバック | 4ファイル | events/[id]/page.tsx, thanks/page.tsx, opengraph-image.tsx, stories-image/route.tsx | OG画像生成時のホスト未取得時のみ作動。本番では headers に host があるため通常は影響なし |

**該当箇所**:
```typescript
// src/app/events/[id]/page.tsx:61
const host = headersList.get("host") ?? "localhost:3007";
```

**判定**: ベストプラクティスとしては `process.env.VERCEL_URL` を使うべきだが、本番動作には支障なし。

### Medium: 14件 🟡
- `console.log` を3ファイルで14件検出
  - `src/app/api/stripe/webhook/route.ts`: 6件（運用ログとして妥当）
  - `src/app/api/events/[id]/cancel/route.ts`: 2件（同上）
  - `src/app/api/events/[id]/export/route.ts`: 6件（**デバッグ残置**：strict環境では削除推奨）

**判定**: webhook/cancel は運用ログとして妥当。**export route の `[CSV Export]` ログだけ過剰** — strict策では削除候補。

### Low: 0件 ✅
- Lorem ipsum なし
- "xxx" / "test123" 等のプレースホルダーパスワード なし
- Not implemented なし

---

## 🟥 2. UI品質レビュー（strict）

### 禁止フォントチェック ✅ PASS
- ❌ Inter: 0件
- ❌ Roboto: 0件
- ❌ Arial: 0件
- ❌ Helvetica: 0件

### 禁止カラー/グラデーションチェック ✅ PASS
- ❌ 紫 → 青グラデーション: 0件
- ❌ 紫 / violet ベースグラデーション: 0件
- ❌ ネオン色: 0件

### 装飾エフェクトチェック 🟡 1件
- 🟡 **Floating particles**（`src/app/page.tsx:908`）
  - 4つの小さなドット（半透明グレー）が浮遊するアニメーション
  - design-requirements.md `anti_patterns.animation` に「particles 禁止」と明記
  - 実態は控えめな装飾だが、**strictでは違反扱い**

### アクセシビリティ 🟡 改善余地
- ✅ `aria-label` を 16コンポーネントで使用（32箇所）
- ⚠️ `text-balance` 使用が見当たらない（design-system.yml で全見出しに必須と規定）
- ✅ `tabular-nums` 22箇所で使用（数値レイアウトで一貫）

### デザインシステム整合性 🟡 大きな乖離
| 項目 | design-system.yml の規定 | 実装 | 評価 |
|---|---|---|---|
| 見出しフォント | Instrument Serif | Noto Serif JP / Josefin Sans | 🟡 規定外（日本語向けには妥当）|
| 本文フォント | Geist Sans | Noto Sans JP / DM Sans | 🟡 規定外（日本語向けには妥当）|
| アクセントカラー | テラコッタ `oklch(0.62 0.18 35)` | 主に黒系 | 🔴 アクセント不在 |
| ダークモード | 必須 | グローバルCSSは設定あり、UI側の対応は限定的 | 🟡 部分実装 |

**判定**: design-system.yml は **理想の将来像** として作成。**現状コードは実装初期の方針が残ったまま**で乖離あり。  
→ Phase 6 デザインリファインフェーズが必要。

### グラデーション使用箇所 ⚠️
- `src/app/[username]/page.tsx`: `bg-gradient-to-br from-[#1A1A1A] via-[#888888] to-[#404040]` 等のモノクロ系グラデーション複数
  - 規定の "no purple-to-blue" には抵触せず
  - ただし design-requirements の "予測可能なレイアウト避ける" には軽く抵触

---

## 🟥 3. パフォーマンス・コード品質

### バンドルサイズ
- 直近のビルド出力: 全38ルート正常生成
- Turbopack コンパイル: 5.5〜10秒で完了

### TypeScript 厳格性 ✅
- `strict: true` 維持
- `any` 使用: 一部 `as unknown as ...` パターンあり（DB型未対応分の暫定処置として許容範囲）

### テストカバレッジ 🔴 致命的
- e2e smoke: **4件のみ** （/, /api/profiles/[username]/follow × 3）
- unit test: **0件**
- integration test: **0件**

→ **Phase 5（テスト）が事実上未実施**。商用稼働サービスとしては大きなリスク。

---

## 🎯 ゲート判定（strict policy）

| カテゴリ | 結果 | 詳細 |
|---|---|---|
| Critical | ✅ PASS | 0件 |
| High | 🟡 注意 | localhost フォールバック1件（実害なし） |
| Medium | 🟡 注意 | console.log 残置14件（うち6件は運用ログ） |
| Low | ✅ PASS | 0件 |
| 禁止フォント/カラー | ✅ PASS | 0件 |
| 禁止アニメーション | 🟡 違反1件 | particles 残置 |
| デザインシステム整合 | 🟡 大幅乖離 | アクセントカラー/フォント要再検討 |
| テスト | 🔴 FAIL | unit/integration 0件 |

### 総合: 🟡 **CONDITIONAL PASS（条件付き合格）**

**本番稼働中のサービスとしては許容**できる状態。ただし以下を**早期対応推奨**：

1. **テスト整備**（最優先・致命傷）
2. console.log の export route 削除
3. design-system.yml と実装の整合化（アクセントカラー導入 OR design-system修正）
4. Floating particles 除去 OR design-requirements の anti-patterns から particles 除外

---

## 📋 推奨アクション

### 🚨 緊急（24時間以内）
- なし

### 🟠 短期（1週間以内）
- [ ] Vitest導入＋主要ロジック（lib/follows, lib/analytics, lib/syllabus）のunit test
- [ ] export route の console.log を構造化ロガーに置換（or 削除）
- [ ] localhost:3007 フォールバックを `process.env.VERCEL_URL` ベースに変更

### 🟡 中期（1ヶ月以内）
- [ ] design-system.yml の規定値を実装に合わせて更新（あるいは逆）
- [ ] テラコッタアクセントカラーの導入（Pro特典・通知バッジ等で）
- [ ] page.tsx の particles 除去 OR コンセプト見直し
- [ ] Phase 5.5 の継続実行を CI に組み込み

---

*Generated by manual strict-policy review on 2026-05-09*
